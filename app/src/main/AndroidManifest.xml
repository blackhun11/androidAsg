<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.feriantosuryawijay.a1901507074_feriantosuryawijaya">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar">
        <activity
            android:name=".LoginActivity"
            android:label="Login" />

        <activity
            android:name=".RegisterActivity"
            android:label="Register" />

        <activity
            android:name=".ViewDollActivity"
            android:label="View Doll"
            android:theme="@style/AppTheme"/>

        <activity
            android:name=".SplashScreen"
            android:label="BlueDoll">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ModifyDollActivity"
            android:label="Add / Modify Doll"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".ViewDollDetailActivity"
            android:label="View Doll Detail"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>